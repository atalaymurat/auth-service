name: Legacy Home Server Deploy Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain
        run: |
          echo "Home-server deploy is disabled."
          echo "Auth service now deploys from Render."
